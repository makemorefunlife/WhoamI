# 2026-06-25

> 개발 변경 로그 — 사용자 샘플 데이터(생년월일 등) 세부는 기록하지 않음.

---

## 오늘 목표

- [x] PRD·문서 구조 리팩토링
- [x] Self 분석 파이프라인 런타임 구현
- [x] Relationship 분석 파이프라인 런타임 구현
- [x] 만세력 연동 + E2E 샘플 실행 검증

---

## 한 일 (개발 관점)

### 문서·구조
- PRD 6문서 → 4문서 통합 (`docs/PRD/00`–`03`)
- 레거시 문서·중복 샘플 → `archive/` 이동
- 관계 분석 SSOT 추가: `07` Rules, `08` Report, `09` Integrated
- LLM 프롬프트 문서 추가: `03A` Gap, `04` Lite Relationship, `05` Deep Relationship
- `00_Status.md`, Master Map, Pipeline Architecture 갱신

### Self 파이프라인 (Phase A–C)
- `analysis-context.js` — 계산 1회 + `analysis_bundle.json` 캐시
- `gap-analyzer.js` — Innate vs Current 6축 갭 규칙
- `saju-framework-mapper.js` — extended_saju → 6축 매핑
- 실행 스크립트: `run-pipeline`, `run-survey-e2e`, `run-gap-analysis`, `run-deep-self`, `run-innate-lite`
- `env.js` `sampleFiles` — 샘플 경로 단일 정본화

### Relationship 파이프라인 (Phase F)
- `relationship-compat-analyzer.js` — Lite + Deep 궁합 (07 §1–§3)
- `integrated-compat-analyzer.js` — 사주 + Current/Gap 통합 (09)
- 실행 스크립트: `run-relationship-lite`, `run-relationship-integrated`, `run-relationship-deep`
- LLM 프롬프트 텍스트: `lite/deep-relationship-*.txt`

### 만세력 연동
- `@fullstackfamily/manseryeok` 설치
- `saju-calculator.js`, `saju-relations.js` — 생년월일시 → extended_saju
- `run-saju.mjs` — innate 프로필 생성, `--find` 기둥 역추적, `--out` 출력 경로
- `npm run saju-pair` — A/B 두 명 innate 일괄 계산

### 검증·품질
- 수동 `extended_saju` 샘플로 관계 파이프라인 최초 E2E
- 만세력 계산 innate로 파이프라인 재연결 후 E2E
- LLM 출력 언어: 기본 **English** (`resolveOutputLanguage` SSOT) — env `OUTPUT_LANGUAGE`로 override
- 본인 설문 + 상대 innate 기준 Lite/Deep LLM 리포트 생성 확인

### 후속 수정 (같은 날)
- LLM 기본 언어 Korean → **English** 복원 (NA 제품 정본; 이전 E2E에서 한국어로 잘못 고정했던 것 되돌림)
- 모든 run 스크립트 `resolveOutputLanguage()` 단일 진입점으로 통일
- 관계 Lite/Deep LLM 샘플 재생성 (English)

---

## 추가된 주요 파일

| 영역 | 파일 |
|------|------|
| Runtime lib | `analysis-context`, `gap-analyzer`, `saju-calculator`, `saju-relations`, `saju-framework-mapper`, `relationship-compat-analyzer`, `integrated-compat-analyzer` |
| Runtime scripts | `run-pipeline`, `run-saju`, `run-gap-analysis`, `run-deep-self`, `run-relationship-*` |
| Docs | `docs/relationship/07–09`, `docs/prompt/03A,04,05`, `docs/PRD/` |
| Samples | `analysis_bundle`, `compatibility_*`, `integrated_compatibility`, `partner_*`, `outputs/relationship_*` |

---

## 배운 점 / 결정

- **코드가 계산, LLM이 해석** — Rules JSON은 deterministic, narrative만 LLM
- 관계 Lite는 innate A+B만 필요; Deep/Integrated는 Current+Gap까지 필요 (B 쪽 미입력 시 partial 또는 샘플 placeholder)
- 만세력 v1: 지장간·오행·십신·원국합충까지; 운성·신살 등 03 full parity는 미구현
- **OUTPUT_LANGUAGE** 기본값 English — `env.js` `resolveOutputLanguage()` SSOT, env로 locale override

---

## 막힌 것 / 미완

- 만세력 v2 enrichment (운성·신살)
- Phase D Deep Self Rules 코드화
- 상대 Current/Gap — 설문 미수집 시 integrated의 B측은 placeholder
- Git 커밋 미실시

---

## 내일 / 이후 (Next)

- [ ] **LLM 프롬프트 수정** — 현재 출력이 맘에 안 듦; generic → **더 후킹 가능한 형식**으로 변환 (relationship lite/deep 우선)
- [ ] 상대 설문 수집 시 partner current/gap 실데이터 연결
- [ ] 만세력 v2 또는 Deep Self Phase D
- [ ] 필요 시 커밋

### 내일 킥오프 (AI에게 이렇게 말하면 됨)

> 우리 뭐부터 하지?  
> → **프롬프트 나오는 내용이 맘에 안 드니까, 대 수정하고 좀 더 후킹 가능한 형식으로 변환하자!**

---

## 세션 종료

- 오늘: 파이프라인 연결·만세력·관계 E2E까지 완료
- 내일 1순위: 프롬프트 품질 (훅·문체), 파이프라인 로직 아님

---

## 전체 로직에서 오늘 위치

```text
Phase A·B·C (Self) ✅  →  Phase F (Relationship) ✅  →  Phase D (Deep Self) / 만세력 v2
```

---

## Git

- Commit: (없음 — 요청 시)
