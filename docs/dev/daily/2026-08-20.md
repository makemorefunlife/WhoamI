# 2026-08-20

---

## 오늘 목표

- [x] 관계 리포트(부부/연인/친구/동료/부모-자녀) 구성 개편 기획 — 부부부터

---

## 한 일

- 사용자가 다른 LLM과 나눈 "관계 리포트 감정 낙차 프레임워크" 브레인스토밍을 공유받아 검토
- 연인/결혼이 이미 완전히 분리된 파이프라인(`lib/relationship/romantic/`, `lib/relationship/marriage/`)임을 재확인 — DEV 7-Scene `domainLenses/partner` 임시 폴백과 혼동하지 말 것
- 부부(Marriage) 리포트의 실제 라이브 렌더 경로가 `MarriageReportView.tsx`의 레거시 카드 JSX가 아니라 `buildMarriageReportViewModel.ts` 기반 Part1-5 뷰모델(`MarriageReportViewModelView`)임을 재확인 — 레거시 JSX는 `section_dna` 누락 시의 폴백일 뿐
- 부부 리포트 신규 섹션 순서 확정 (아래 "배운 점 / 결정" 참고)
- "일상 모습" 콘텐츠는 `marriageSajuGapInsights.ts`의 기존 결정론적 문장(항목1 decisionMakingLine, 항목2 crisisRoleLine, 항목3 drainPatternLine)을 각자 섹션(money_chores/upset/warning)에서 꺼내 승격하는 방향으로 결론 — 신규 LLM 로직 최소화
- "일간 매력/일지 배우자특징/일지 주도권" 3개 신규 표는 기존 코드에 없음을 확인(연인 도메인의 `dayStemRomanticProfile.ts`는 일간 조합 한줄이지 개별 매력 키워드가 아님) — 신규 작성 필요
- "배우자 직업" 표는 이번 스코프에서 제외하기로 결정 (관계적 톤과 결이 달라서)

---

## 수정·완료

| 구분 | 내용 |
|------|------|
| 완료 | 부부 리포트 섹션 재구성 기획 확정 (코드 변경 없음, 순수 기획 세션) |
| 수정 | (없음) |
| 삭제 | (없음) |

---

## 배운 점 / 결정

- 부부 리포트 최종 순서: `헤드라인+트라이스코어 → origin_story(우리가 부부가 된 이유) → 일상 모습(신규 — 일간/일지 표 기반 매력·배우자특징·주도권) → DNA(11축 psych_radar → 사주 compare_table 순서로 스왑) → 테마 무기화(프라이버시→침실→돈→양육→시댁, 감정 무게순) → 오해의 번역기(Warning+DNA 재조합) → 처방(디에스컬레이션+동거처방) → 웨더포캐스트(장기비전, 클로징)`
- React 컴포넌트에 `try { buildViewModel() } catch { 레거시 fallback }` 패턴이 있을 때, fallback JSX만 읽고 "이게 현재 구조"라고 단정하면 안 됨 — 항상 분기 조건(`if (partRendererViewModel)`)부터 확인
- 로맨틱/결혼 도메인은 관계 유형마다 규모가 완전히 다름 — 결혼은 이미 성숙한 단일 파이프라인, 연인은 legacy/V4가 공존하는 과도기 상태

→ `decisions/033_marriage-report-part-reorder.md`

---

## 막힌 것

- (없음)

---

## 내일 (Next)

- [ ] "일상 모습" 신규 3개 표(일간 매력 10개, 일지 배우자특징 12개, 일지 주도권 12개) 콘텐츠 작성
- [ ] 표 조합 → 서사화 레이어 설계 (Part04 story-planner 패턴 재사용 검토)
- [ ] 부부 리포트 실제 구현 착수 여부 결정
- [ ] 연인/동료/친구/부모-자녀 나머지 4개 타입 기획 논의 (동료는 "답답함 인정 후 균형" 오프닝, 부모-자녀는 "관찰형" 오프닝으로 이미 방향 합의됨)

---

## Git

- Commit: (없음 — 기획 논의만, 코드 변경 없음)
