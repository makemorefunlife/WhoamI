/**
 * @deprecated 2026-07-19 — 폐기됨, 아무 곳에서도 import 안 함.
 *
 * 이 파일은 친구 "한눈에 비교" 표의 첫 버전(11축 심리 설문 중심)이었으나,
 * 사용자가 동료(work) 표와 동일하게 **사주 중심**으로 통일하기로 확정하면서
 * `./friendSajuCompareTable.ts`로 완전히 대체됨. 실제 코드 삭제 권한이 없어
 * 이 스텁만 남겨둠 — 가족(family) 도메인 감사에서 겪은 것과 같은 "죽은 파일이
 * 실제로 쓰이는 줄 착각하는" 혼란을 막기 위해 원본 구현은 지우고 이 안내문만
 * 남긴다. 실제 사용처: `./friendSajuCompareTable.ts`의 `buildFriendSajuCompareTable`.
 */
export {};
